% WACV 2027 submission --- MorphoST
% Self-contained: wacv.sty (CVF style) and ieeenat_fullname.bst ship alongside this file, so it
% compiles on Overleaf as-is. Swap in the official WACV 2027 kit files when released.
\documentclass[10pt,twocolumn,letterpaper]{article}
\usepackage[review]{wacv}

\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{array}
\usepackage{xcolor}
\usepackage{makecell}
\usepackage{enumitem}
\usepackage{placeins}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,backgrounds,calc}
\usepackage[pagebackref,breaklinks,colorlinks]{hyperref}

% WACV boilerplate (consumed by wacv.sty for the review header)
\def\paperID{****}
\def\confName{WACV}
\def\confYear{2027}

\newcommand{\morph}{\textsc{MorphoST}}
\newcommand{\best}[1]{{\bfseries\boldmath #1}}
\newcommand{\running}{\multicolumn{1}{c}{\textit{running}}}
% Placeholder box for figures generated from the completed corrected runs.
\newcommand{\figph}[2]{%
  \fcolorbox{gray!40}{gray!5}{\parbox[c][#1][c]{0.98\linewidth}{\centering
  \footnotesize\itshape\color{gray!60!black} #2}}}
\renewcommand{\arraystretch}{1.05}

\begin{document}

\title{\morph: A Simple Coordinate-Invariant Transformer for\\
Generalizable Gene Expression Prediction from Histology}

\author{Anonymous WACV submission\\
Paper ID \paperID}
\maketitle

\begin{abstract}
Spatial transcriptomics measures gene expression while preserving its location in tissue, but the
assay remains expensive. Predicting spatial gene expression directly from routinely collected
hematoxylin-and-eosin (H\&E) images could provide a lower-cost alternative; however, most existing
methods are trained and tested within the same tissue cohort, leaving their ability to generalize to
new organs unclear. We introduce \morph{}, a compact transformer that directly predicts gene
expression at every measured tissue location, or spot, in a slide. The model represents spatial
relationships using only distances between spots rather than absolute coordinates; therefore, with
the histology features held fixed, its spatial computations remain unchanged when the coordinate
system is translated, rotated, or reflected. \morph{} combines nearby tissue information through
distance-aware local attention, slide-wide information through global attention, and an
attention-pooled summary of the entire slide, predicting all target genes in a single forward pass
without generative sampling. We evaluate \morph{} using frozen UNI histology features and matched
data splits, gene panels, training budgets, and validation procedures in three settings: within-organ
prediction using an intra-cohort protocol, pooled cross-validation on new samples from known organs,
and leave-one-organ-out transfer to an unseen organ. On HEST-1k, \morph{} achieves the best average
within-organ performance among five feature-matched baselines, providing a $6.6\%$ relative
improvement in Pearson correlation over the strongest baseline, while remaining competitive under
pooled and leave-one-organ-out evaluation. With $4.75$M parameters and single-pass inference,
\morph{} provides a simple and efficient approach to spatial gene-expression prediction across
diverse tissue types.
\end{abstract}

\section{Introduction}
Spatial transcriptomics (ST) measures gene expression together with its location in tissue, making
it possible to study spatial heterogeneity, tissue organization, and interactions between cells
\cite{staahl2016visualization}. Despite its value, ST remains costly and experimentally demanding.
Hematoxylin-and-eosin (H\&E) whole-slide images are routinely collected and contain morphological
patterns associated with tissue composition and molecular activity. This has motivated computational
methods that predict spatial gene expression from histology, potentially extending molecular analysis
to samples for which ST measurements are unavailable.

Most histology-to-expression methods are developed and evaluated separately within individual
tissue cohorts. In this setting, training and test slides share an organ type and often similar
collection characteristics. Strong within-cohort performance therefore does not establish that a
model will generalize across organs. Such transfer is challenging because organs differ in tissue
architecture, cell composition, expression distributions, staining, and acquisition conditions. We
address this gap with two multi-organ evaluations: pooled cross-validation tests held-out samples
from organ types represented during training, whereas leave-one-organ-out (LOOO) evaluation tests an
entirely unseen organ (Fig.~\ref{fig:teaser}). Together with standard intra-cohort evaluation, these
settings separate within-organ prediction, generalization to new samples from known organs, and
transfer to a new organ.

Existing methods capture spatial context using graph networks, large attention models, generative
objectives, or explicit count-distribution priors. These approaches can be effective, but their
additional complexity makes it harder to determine which spatial signals are necessary for accurate
and transferable prediction. We instead ask whether a compact direct-regression model can provide a
strong baseline when it is given a suitable geometric inductive bias. Our central design choice is
to use coordinates only through pairwise Euclidean distances. With histology features held fixed,
this makes the spatial pathway unchanged by translations, rotations, and reflections of the
coordinate system, without requiring frame averaging.

We introduce \morph{} (\textbf{Morpho}logy-aware \textbf{S}patial \textbf{T}ransformer), a
single-pass model that predicts a panel of genes at every spot in a slide. Each layer combines three
complementary forms of context: distance-aware local attention captures relationships between nearby
spots, global self-attention models slide-wide dependencies, and an attention-pooled slide
representation summarizes overall morphology. A linear prediction head then produces expression for
all spots and genes in one forward pass, without generative sampling. This design is compact,
coordinate-invariant in its spatial pathway, and directly testable through component ablations.

\smallskip\noindent\textbf{Research questions.} We organize the study around five questions:
\begin{itemize}[leftmargin=1.5em,itemsep=0pt,topsep=2pt]
  \item[\textbf{RQ1}] Can direct regression compete under standard within-cohort evaluation?
  \item[\textbf{RQ2}] Does \morph{} generalize across organs without per-organ training?
  \item[\textbf{RQ3}] Which spatial context components drive performance?
  \item[\textbf{RQ4}] Do predictions recover biological spatial structure beyond per-gene correlation?
  \item[\textbf{RQ5}] How efficient and scalable is the model?
\end{itemize}

\smallskip\noindent\textbf{Contributions.}
\textbf{(1)} We propose a compact spatial transformer that combines local, global, and slide-level
context while using only pairwise distances for spatial encoding. \textbf{(2)} We establish a
feature-matched evaluation protocol with identical frozen UNI features, data splits, gene panels,
training budgets, and nested validation across intra-cohort, POOLED, and LOOO settings on HEST-1k.
Under intra-cohort evaluation, \morph{} improves average Pearson correlation by $6.6\%$ relative to
the strongest of five controlled baselines and remains competitive in both multi-organ settings.
\textbf{(3)} We analyze the model through factorial context and objective ablations, spatial domain
recovery, representation and expression-map visualizations, numerical invariance checks, and measured
inference scaling.

% ------------------------------------------------------------------ teaser
\begin{figure}[t]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[font=\scriptsize,
  organ/.style={draw,rounded corners=1.5pt,minimum width=0.62cm,minimum height=0.42cm,inner sep=0pt},
  lab/.style={font=\scriptsize\bfseries},
  arr/.style={-{Latex[length=1.4mm]},semithick}]

% ---- left panel: intra-cohort ----
\node[lab] at (1.55,2.35) {Intra-cohort (standard)};
\foreach \i in {0,1,2,3} { \node[organ,fill=blue!12] at (0.55+0.72*\i,1.7) {}; }
\node at (1.55,1.15) {train / test \emph{within} one organ};
\node[organ,fill=blue!12] (a1) at (0.9,0.45) {};
\node[organ,draw=red!70!black,very thick,fill=red!12] (a2) at (2.2,0.45) {};
\node at (0.9,0.03) {train}; \node at (2.2,0.03) {test};

% ---- right panel: cross-organ ----
\node[lab] at (5.35,2.35) {Cross-organ (this work)};
\foreach \i/\c in {0/orange,1/green,2/teal,3/purple,4/brown,5/olive,6/cyan}
  { \node[organ,fill=\c!18] at (4.05+0.42*\i,1.7) {}; }
\node at (5.35,1.15) {train on 7 organs $\rightarrow$ test held-out organ};
\node[organ,fill=orange!18] (b1) at (4.55,0.45) {};
\node at (5.05,0.45) {$\cdots$};
\node[organ,fill=green!18] (b2) at (5.55,0.45) {};
\node[organ,draw=red!70!black,very thick,fill=red!12] (b3) at (6.6,0.45) {};
\node at (5.05,0.03) {seen organs}; \node at (6.6,0.03) {unseen};

\draw[dashed,gray] (3.35,-0.15) -- (3.35,2.55);

% ---- bottom message ----
\node[draw=blue!45,rounded corners=3pt,fill=blue!4,align=center,text width=7.1cm,inner sep=4pt]
  at (3.55,-0.85)
  {\footnotesize Complex generative baselines are tuned per-cohort and can degrade under organ
   shift. \morph{} is a single simple coordinate-invariant regressor evaluated \emph{unchanged} in both
   regimes.};
\end{tikzpicture}}
\caption{\textbf{Two evaluation regimes.} Standard benchmarks train and test within one tissue cohort
(left). We additionally test the harder cross-organ transfer (right), where a model must predict
expression for slides from organs unseen during training. \morph{} is evaluated in both without
per-cohort redesign.}
\label{fig:teaser}
\end{figure}

\begin{figure*}[t]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[font=\footnotesize,
  io/.style={draw,very thick,rounded corners=2pt,align=center,minimum height=1.25cm,text width=2.65cm},
  stage/.style={draw,rounded corners=2pt,align=center,minimum height=0.78cm,text width=2.35cm},
  path/.style={draw,rounded corners=2pt,align=center,minimum height=1.05cm,text width=2.45cm},
  claim/.style={draw,very thick,rounded corners=3pt,align=left,minimum height=1.05cm,text width=4.55cm,
                inner xsep=7pt,inner ysep=5pt},
  badge/.style={circle,draw=white,very thick,fill=blue!65!black,text=white,font=\bfseries,
                minimum size=6.5mm,inner sep=0pt},
  arr/.style={-{Latex[length=1.8mm]},semithick},
  flow/.style={-{Latex[length=2.2mm]},very thick}]

% Input and tokenization
\node[io,fill=gray!9] (wsi) at (0,1.05) {\textbf{H\&E whole-slide image}\\spot-centered morphology};
\node[io,fill=gray!9] (coord) at (0,-0.55) {\textbf{Spot coordinates}\\$\mathbf p_i\in\mathbb R^2$};
\node[stage,fill=orange!10] (uni) at (3.35,1.05) {frozen UNI encoder\\$\mathbf f_i\in\mathbb R^{1024}$};
\node[stage,fill=green!10] (dist) at (3.35,-0.55) {pairwise distances\\$D_{ij}=\|\mathbf p_i-\mathbf p_j\|_2$};
\node[stage,fill=blue!7,text width=2.25cm,minimum height=1.2cm] (token) at (6.15,0.25)
  {spot tokens\\$\mathbf x\in\mathbb R^{N\times d}$};

\draw[flow](wsi)--(uni);
\draw[flow](coord)--(dist);
\draw[flow](uni)--(token);
\draw[flow](dist)--(token);

% MorphoST block
\node[path,fill=blue!8] (local) at (9.25,1.55) {\textbf{Local context}\\distance-biased $k$NN attention};
\node[path,fill=violet!8] (global) at (9.25,0.25) {\textbf{Global context}\\slide-wide self-attention};
\node[path,fill=cyan!9] (slide) at (9.25,-1.05) {\textbf{Slide context}\\attention-pooled summary};
\node[stage,fill=gray!8,text width=2.35cm,minimum height=1.15cm] (fusion) at (12.25,0.25)
  {residual fusion\\LayerNorm $+$ FFN\\$\times L$};

\draw[arr](token.east)--(local.west);
\draw[arr](token.east)--(global.west);
\draw[arr](token.east)--(slide.west);
\draw[arr](local.east)--(fusion.west);
\draw[arr](global.east)--(fusion.west);
\draw[arr](slide.east)--(fusion.west);

\begin{scope}[on background layer]
\node[draw=blue!55!black,dashed,rounded corners=3pt,fit=(local)(global)(slide)(fusion),
      inner sep=7pt,fill=blue!2] (model) {};
\end{scope}
\node[anchor=south west,font=\small\bfseries,text=blue!55!black] at ($(model.north west)+(0.05,0.04)$)
  {MorphoST spatial transformer};

% Output
\node[io,fill=red!7,text width=2.65cm] (out) at (15.55,0.25)
  {\textbf{Predicted expression map}\\$\hat{\mathbf Y}\in\mathbb R^{N\times G}$\\one value per-spot and gene};
\draw[flow](fusion)--(out);

% Contribution strip
\node[font=\small\bfseries,text=blue!60!black] (ctitle) at (8.0,-2.15)
  {OUR CONTRIBUTIONS};
\node[claim,draw=green!55!black,fill=green!8] (c1) at (2.35,-3.18)
  {\hspace{5mm}\textbf{Invariant spatial encoding}\\\hspace{5mm}Distances replace absolute positions, yielding a coordinate-invariant spatial pathway.};
\node[badge,fill=green!55!black] at ($(c1.west)+(0.13,0)$) {1};
\node[claim,draw=orange!75!black,fill=orange!9] (c2) at (8.00,-3.18)
  {\hspace{5mm}\textbf{Simple whole-slide regression}\\\hspace{5mm}Local, global, and slide context produce all gene maps in one forward pass.};
\node[badge,fill=orange!75!black] at ($(c2.west)+(0.13,0)$) {2};
\node[claim,draw=violet!65!black,fill=violet!9] (c3) at (13.65,-3.18)
  {\hspace{5mm}\textbf{Controlled evidence}\\\hspace{5mm}Two regimes, two benchmarks, three metrics, ablations, and scaling.};
\node[badge,fill=violet!65!black] at ($(c3.west)+(0.13,0)$) {3};

% Link each contribution to the corresponding evidence in the pipeline.
\draw[green!55!black,densely dotted,semithick] (dist.south) to[out=-90,in=90] (c1.north);
\draw[orange!75!black,densely dotted,semithick] (fusion.south) to[out=-90,in=90] (c2.north);
\draw[violet!65!black,densely dotted,semithick] (out.south) to[out=-90,in=90] (c3.north);

\begin{scope}[on background layer]
\node[draw=blue!25,rounded corners=4pt,fill=blue!1,fit=(ctitle)(c1)(c2)(c3),inner sep=8pt] {};
\end{scope}

\end{tikzpicture}
}
\caption{\textbf{MorphoST overview and contributions.} The model receives frozen UNI morphology
features and spot coordinates. Coordinates affect the network only through pairwise distances. Each
layer combines local, global, and slide-level context before a linear head predicts the expression of
$G$ genes at all $N$ spots. The design provides an invariant spatial pathway, direct single-pass
inference, and a controlled framework for evaluating which forms of spatial context are useful.}
\label{fig:arch}
\end{figure*}

\section{Related Work}

\subsection{Histology-based gene expression prediction}
Histology-to-expression methods differ mainly in how they represent morphology and exchange
information between spatial locations. ST-Net \cite{he2020stnet} predicts expression from individual
image patches with a convolutional network, providing a strong spot-level formulation but no
explicit interaction between spots. HisToGene \cite{pang2021histogene} introduces a transformer to
model dependencies across a slide, while Hist2ST \cite{zeng2022hist2st} combines transformer layers
with a spatial graph. BLEEP \cite{xie2023bleep} instead aligns histology and expression embeddings
through contrastive learning and predicts by retrieval. TRIPLEX \cite{chung2024triplex} integrates
spot, neighbourhood, and global image information at multiple resolutions. These methods demonstrate
that spatial and contextual information improves upon independent patch regression, but they use
substantially different features, objectives, and evaluation procedures in their original studies.

Recent methods introduce additional structural assumptions. MCToGene \cite{sun2026mctogene} models
higher-order interactions among multiple cells. Gene-DML \cite{song2026genedml} uses dual pathways
and multi-level discrimination, whereas HyperST \cite{zhang2026hyperst} represents cross-modal
structure in hyperbolic space. Large pretrained models such as STPath \cite{stpath2025} learn across
organs and platforms with explicit metadata. \morph{} is complementary to these approaches: it uses
frozen UNI features \cite{chen2024uni}, direct regression, and a compact spatial transformer so that
the effects of local, global, and slide-level context can be evaluated under a common protocol.

\subsection{Geometric structure in spatial models}
Spatial prediction depends on the coordinate representation supplied to a model. Absolute position
embeddings can encode location within a particular coordinate frame, but their output may change
when an equivalent tissue layout is translated, rotated, or reflected. Geometric networks address
this issue by constructing interactions from relative quantities \cite{satorras2021egnn}. \morph{}
uses an invariant construction in which coordinates enter its spatial pathway only through pairwise
Euclidean distances. Because these distances are preserved by translations, rotations, and
reflections, no transformed coordinate frames or prediction averaging are required. This guarantee
applies to the spatial pathway for fixed histology features; it does not imply that the frozen image
encoder is invariant to rotations of the image itself.

\subsection{Generalization across samples and organs}
Most published spatial gene-expression benchmarks train and test within the same tissue cohort.
HEST-1k \cite{jaume2024hest} provides data from multiple cohorts and organs, but its standard
evaluation remains organized by cohort. Consequently, a high within-cohort score can reflect
adaptation to organ-specific morphology, expression distributions, or acquisition conditions.
Recent benchmarking work likewise identifies cross-sample generalization as a major challenge
\cite{storm2025benchmark}. We evaluate two complementary multi-organ settings. POOLED
cross-validation trains jointly on all organ groups and tests held-out samples from organ types seen
during training. LOOO trains on seven organ groups and evaluates on the held-out eighth group,
directly measuring transfer to an unseen organ. This distinction prevents performance on known
organs from being interpreted as evidence of unseen-organ generalization.

\section{Method}
\morph{} directly maps the spots of one tissue slide to their gene-expression profiles. For a slide
with $N$ spots, the input consists of frozen UNI features
$\mathbf{F}=[\mathbf{f}_1,\ldots,\mathbf{f}_N]^\top\in\mathbb{R}^{N\times 1024}$ and spatial
coordinates $\mathbf{P}=[\mathbf{p}_1,\ldots,\mathbf{p}_N]^\top\in\mathbb{R}^{N\times 2}$. The
output $\hat{\mathbf{Y}}\in\mathbb{R}^{N\times G}$ contains the predicted log-normalized expression
of a $G$-gene panel at every spot. Slides are processed independently, so the model requires neither
cross-slide padding nor a fixed number of spots.

\subsection{Coordinate-invariant spatial encoding}
We first compute the pairwise Euclidean distance matrix
$D_{ij}=\lVert\mathbf{p}_i-\mathbf{p}_j\rVert_2$. For each spot $i$, we retain the indices
$\mathcal{N}(i)$ and distances of its $k$ nearest neighbours, excluding the spot itself. Distances
are expanded into $M$ radial-basis features,
\begin{equation}
\boldsymbol{\phi}(D_{ij})=
\left[\exp\!\left(-\gamma_m D_{ij}^{2}\right)\right]_{m=1}^{M},
\end{equation}
where the fixed bandwidth parameters $\gamma_m$ are logarithmically spaced. These features influence
attention weights but are never added to the spot embeddings as absolute positions.

This construction yields coordinate-system invariance. For any orthogonal matrix $R$ and translation
$\mathbf{t}$,
\begin{equation}
\lVert R\mathbf{p}_i+\mathbf{t}-(R\mathbf{p}_j+\mathbf{t})\rVert_2
=\lVert\mathbf{p}_i-\mathbf{p}_j\rVert_2.
\end{equation}
The neighbour graph, radial-basis features, and predictions are therefore unchanged by translations,
rotations, and reflections of the coordinates, up to floating-point error, when the UNI features are
held fixed. The result concerns transformations of the coordinate system and does not assert
rotation invariance of the frozen histology encoder.

\subsection{The \morph{} layer}
The input features are projected to hidden width $d$,
$\mathbf{X}^{(0)}=\mathbf{F}W_{\mathrm{in}}+\mathbf{b}_{\mathrm{in}}$. The same neighbour graph and
radial-basis features are reused in all $L$ transformer layers. At layer $\ell$, a pre-normalized
representation $\mathbf{H}=\operatorname{LN}(\mathbf{X}^{(\ell)})$ is passed through three context
pathways.

\noindent\textbf{Local distance-biased attention.} Each spot attends to its $k$ neighbours with a
learned, distance-dependent bias for each attention head. For head $h$, the attention weights are
\begin{equation}
\alpha_{ij}^{h}=\operatorname{softmax}_{j\in\mathcal{N}(i)}\!\left(
\frac{(\mathbf{q}_i^{h})^\top\mathbf{k}_j^{h}}{\sqrt{d_h}}
+b_h\!\left(\boldsymbol{\phi}(D_{ij})\right)\right),
\end{equation}
where $d_h=d/H$ for $H$ heads. The local output is the concatenation of the weighted value sums,
followed by a learned output projection. Morphological similarity is captured by the query--key
term, while spatial distance contributes through the learned radial-basis bias.

\noindent\textbf{Global self-attention.} In parallel, standard multi-head self-attention
\cite{vaswani2017attention} allows every spot to attend to all $N$ spots. This pathway captures
long-range dependencies that may not be connected in the local graph. It uses morphology features
only and is permutation-equivariant with respect to spot order.

\noindent\textbf{Attention-pooled slide context.} A learned scalar scoring function produces
$a_i=\operatorname{softmax}_{i}(s(\mathbf{h}_i))$. The slide representation is
\begin{equation}
\mathbf{g}=W_g\left(\sum_{i=1}^{N}a_i\mathbf{h}_i\right)+\mathbf{b}_g,
\end{equation}
and is broadcast to every spot. Unlike a learned class token, this representation is computed
directly from the slide's spot features and summarizes its morphological composition.

For the complete V3 model, the local output, global output, and broadcast slide representation are
summed and added to the residual stream. A second pre-normalization residual update applies a
two-layer feed-forward network with expansion factor four and GELU activation:
\begin{align}
\widetilde{\mathbf{X}}^{(\ell)}
  &=\mathbf{X}^{(\ell)}+\mathbf{L}^{(\ell)}+\mathbf{A}^{(\ell)}+\mathbf{G}^{(\ell)},\\
\mathbf{X}^{(\ell+1)}
  &=\widetilde{\mathbf{X}}^{(\ell)}+
    \operatorname{FFN}\!\left(\operatorname{LN}(\widetilde{\mathbf{X}}^{(\ell)})\right).
\end{align}
After $L$ layers, layer normalization and a linear head map each spot representation to its $G$
predicted gene values. The model produces the complete slide prediction in one forward pass.

\subsection{Training objective and staged design}
\label{sec:loss}
For a slide with measured expression $\mathbf{Y}$, we optimize mean squared error together with a
gene-wise Pearson-correlation term:
\begin{equation}
\mathcal{L}=\mathrm{MSE}(\hat{\mathbf{Y}},\mathbf{Y})
+ \lambda\left(1-\frac{1}{|\mathcal{V}|}\sum_{g\in\mathcal{V}}\rho_g\right),
\end{equation}
where $\rho_g$ is the Pearson correlation between predicted and measured expression across the $N$
spots, $\mathcal{V}$ is the set of genes with nonzero target variance on that slide, and
$\lambda=0.5$. MSE constrains the scale of the predictions, while the correlation term encourages
the model to reproduce spot-to-spot expression patterns.

We developed the architecture incrementally: \textbf{V0} retains the projection, residual
feed-forward blocks, and prediction head but removes all context pathways; \textbf{V1} adds global
self-attention; \textbf{V2} adds local distance-biased attention; and \textbf{V3} adds the pooled
slide representation. V3 is the complete model used in the main experiments. To avoid attributing
performance to this particular addition order, the factorial ablation independently enables or
disables the local (L), global (G), and slide (S) pathways (Table~\ref{tab:ablation}).

% Declared on page 4 so the two-column float appears at the top of page 5 with RQ1.
\begin{table*}[!t]
\centering
\caption{\textbf{Intra-cohort gene expression prediction on HEST-1k, grouped by organ (Pearson
$\uparrow$).} Mean$_{\mathrm{std}}$ over three seeds with nested-validation; best per row in \best{bold}.
All methods use identical frozen UNI features, outer splits, 50-gene panels, and the same training
budget (100 epochs, patience 20, inner-validation selection). \morph{} attains the best correlation on
every organ group and on the average. Published per-cohort results are in
Appendix~\ref{app:published}. Gene-DML \cite{song2026genedml} and HyperST \cite{zhang2026hyperst} are our feature-matched
reproductions of recent (2025--2026) methods.}
\label{tab:main}
\footnotesize
\setlength{\tabcolsep}{3.2pt}
\begin{tabular}{l ccc cc c}
\toprule
Organ & ST-Net~\tiny{'20} & HisToGene~\tiny{'21} & Hist2ST~\tiny{'22} & Gene-DML~\tiny{'26} & HyperST~\tiny{'26} & \morph{} (V3) \\
\midrule
Breast      & $0.364_{0.01}$ & $0.363_{0.04}$ & $0.277_{0.03}$ & $0.396_{0.00}$ & $0.398_{0.00}$ & \best{$0.440_{0.00}$} \\
Prostate    & $0.251_{0.01}$ & $0.332_{0.00}$ & $0.317_{0.02}$ & $0.363_{0.01}$ & $0.369_{0.00}$ & \best{$0.390_{0.02}$} \\
Pancreas    & $0.353_{0.02}$ & $0.307_{0.03}$ & $0.243_{0.06}$ & $0.362_{0.04}$ & $0.367_{0.04}$ & \best{$0.392_{0.04}$} \\
Skin        & $0.632_{0.01}$ & $0.554_{0.01}$ & $0.509_{0.02}$ & $0.674_{0.00}$ & $0.641_{0.00}$ & \best{$0.696_{0.00}$} \\
Colorectal  & $0.181_{0.01}$ & $0.190_{0.01}$ & $0.195_{0.01}$ & $0.222_{0.01}$ & $0.223_{0.00}$ & \best{$0.243_{0.01}$} \\
Kidney      & $0.172_{0.01}$ & $0.210_{0.01}$ & $0.217_{0.01}$ & $0.224_{0.03}$ & $0.256_{0.00}$ & \best{$0.265_{0.02}$} \\
Liver       & $0.056_{0.01}$ & $0.029_{0.00}$ & $0.031_{0.01}$ & $0.077_{0.01}$ & $0.087_{0.01}$ & \best{$0.100_{0.00}$} \\
Lung        & $0.482_{0.01}$ & $0.490_{0.00}$ & $0.378_{0.10}$ & $0.564_{0.00}$ & $0.581_{0.01}$ & \best{$0.589_{0.01}$} \\
\midrule
\textbf{Average} & $0.311$ & $0.309$ & $0.271$ & $0.360$ & $0.365$ & \best{$0.389$} \\
\bottomrule
\end{tabular}
\end{table*}

\begin{table*}[!t]
\centering
\caption{\textbf{Intra-organ gene expression prediction on mouse HEST (Pearson $\uparrow$).}
5-fold cross-validation within each mouse organ; mean$_{\mathrm{std}}$ over three seeds with nested-validation.
59 Visium slides across 7 mouse organs from the MahmoodLab HEST collection~\cite{jaume2024hest}.
All methods use identical frozen UNI features and organ-specific 50-gene HVG panels.
Best per row in \best{bold}.}
\label{tab:mouse_intra}
\footnotesize
\setlength{\tabcolsep}{3.2pt}
\begin{tabular}{l ccccc c}
\toprule
Organ & ST-Net~\tiny{'20} & HisToGene~\tiny{'21} & Hist2ST~\tiny{'22} & TRIPLEX~\tiny{'24} & BLEEP~\tiny{'23} & \morph{} (V3) \\
\midrule
Brain   & \running & \running & \running & \running & \running & \running \\
Liver   & \running & \running & \running & \running & \running & \running \\
Skin    & \running & \running & \running & \running & \running & \running \\
Heart   & \running & \running & \running & \running & \running & \running \\
Bowel   & \running & \running & \running & \running & \running & \running \\
Kidney  & \running & \running & \running & \running & \running & \running \\
Muscle  & \running & \running & \running & \running & \running & \running \\
\midrule
\textbf{Average} & \running & \running & \running & \running & \running & \running \\
\bottomrule
\end{tabular}
\end{table*}

\section{Experiments}

\subsection{Datasets}
\label{sec:datasets}
We evaluate on \textbf{HEST-1k} \cite{jaume2024hest}, which provides ten tissue
cohorts, each with a patient-stratified $k$-fold split; for presentation we aggregate the ten cohorts
into eight organ groups (breast $=$ IDC $+$ LYMPH\_IDC, colorectal $=$ COAD $+$ READ, others one
cohort each). For every slide we predict the 50 most highly
variable genes after $\log(1+x)$ normalization, using identical frozen UNI \cite{chen2024uni}
features across all methods.

\subsection{Experimental setup}
\label{sec:setup}
\textbf{Two regimes.} We evaluate under (i) an \emph{intra-cohort} protocol---the standard per-cohort
$k$-fold cross-validation provided with the HEST-1k benchmark \cite{jaume2024hest}, training
separately within each cohort---and (ii) a harder \emph{cross-organ} protocol with two
sample-disjoint settings:
leave-one-organ-out (\textsc{looo}), where seven organ groups train and the eighth tests, and pooled
cross-validation (\textsc{pooled}). Patient metadata are unavailable for some cohorts, so we do not
claim the pooled splits are patient-disjoint.

\textbf{Metrics and protocol.} We report the Pearson correlation coefficient (PCC), Spearman
correlation (SCC), and mean squared error (MSE) between predicted and measured expression, averaged
over folds and three random seeds (mean$_{\mathrm{std}}$). Every model selects its checkpoint on an
inner-validation split of the training fold and is evaluated once on the outer test-fold; where a
cohort fold contains a single training slide, the inner split holds out a disjoint subset of that
slide's spots. This removes the test-fold model selection present in earlier exploratory runs.

\textbf{Baselines.} We compare against ST-Net \cite{he2020stnet}, HisToGene \cite{pang2021histogene},
Hist2ST \cite{zeng2022hist2st}, Gene-DML \cite{song2026genedml},
and HyperST \cite{zhang2026hyperst}. All
methods use identical frozen UNI features, data splits, and 50-gene panels; the prediction architecture
is the principal varying factor. For reference, the published per-cohort accuracy of the
higher-order method MCToGene \cite{sun2026mctogene} is summarized in
Appendix~\ref{app:published}; it uses the ungrouped ten-cohort split and is not directly comparable
to our feature-matched, nested-validation numbers.

\textbf{Implementation.} \morph{} uses $L{=}4$ layers, hidden width $d{=}256$, $4$ attention heads,
$k{=}8$ neighbours, and $16$ radial-basis functions ($4.8$M parameters). We train per slide with AdamW
(learning rate $10^{-3}$, weight decay $10^{-2}$), a cosine schedule, and gradient clipping at $1.0$,
for at most $100$ epochs with early-stopping patience of $20$; each step samples at most $3{,}000$
spots. Experiments run on one NVIDIA A100 GPU.

\subsection{RQ1: Can simple direct-regression compete on the standard benchmark?}
\label{sec:rq1}
\noindent\textbf{Answer: Yes---under controlled conditions \morph{} leads all five baselines on every organ and metric.}
Under our feature-matched, nested-validation protocol, \morph{} attains the best per-organ Pearson
correlation on all eight HEST-1k organ groups and leads on every metric in the three-metric summary
(Table~\ref{tab:metrics}: PCC $0.389$, SCC $0.348$, MSE $1.072$; next-best HyperST $0.365$).
The per-organ breakdown is in Table~\ref{tab:main}.
For context, Appendix~\ref{app:published} reports published per-cohort numbers from the original
HEST-1k split, including MCToGene \cite{sun2026mctogene}; those figures use ten cohorts without
organ grouping and their original (not nested) selection protocol.
On the standard split, larger and higher-order models score above a compact direct-regression model
as expected; our finding is that under controlled conditions \morph{} consistently leads the same
five baselines while using one forward pass and $4.8$M parameters---far fewer than HisToGene ($149$M).

\begin{table}[t]
\centering
\caption{\textbf{Intra-cohort three-metric summary} (HEST-1k, averaged over the eight organ groups;
three seeds). PCC$\uparrow$, SCC$\uparrow$, MSE$\downarrow$. Best per column in \best{bold}.}
\label{tab:metrics}
\small
\setlength{\tabcolsep}{5pt}
\begin{tabular}{l ccc}
\toprule
Method & PCC $\uparrow$ & SCC $\uparrow$ & MSE $\downarrow$ \\
\midrule
ST-Net~\cite{he2020stnet} (2020)      & 0.311 & 0.269 & 1.633 \\
HisToGene~\cite{pang2021histogene} (2021)   & 0.309 & 0.273 & 1.172 \\
Hist2ST~\cite{zeng2022hist2st} (2022)     & 0.271 & 0.250 & 1.292 \\
Gene-DML~\cite{song2026genedml} (2026)    & 0.360 & 0.323 & 1.113 \\
HyperST~\cite{zhang2026hyperst} (2026)     & 0.365 & 0.329 & 1.113 \\
\midrule
\morph{} (V3) & \best{0.389} & \best{0.348} & \best{1.072} \\
\bottomrule
\end{tabular}
\end{table}

\FloatBarrier
\subsection{RQ2: Does MorphoST generalize across organs without per-organ training?}
\label{sec:rq2}
\noindent\textbf{Answer: Yes---\morph{} is competitive under both LOOO and pooled transfer, on both benchmarks.}
We evaluate two cross-organ regimes on both benchmarks: leave-one-organ-out (\textsc{looo}),
where the model trains on seven organ groups and is tested on the held-out eighth, and pooled
five-fold cross-validation (\textsc{pooled}), where all organs are trained together.
Table~\ref{tab:looo} gives per-organ \textsc{looo} Pearson on HEST-1k;
Table~\ref{tab:pooled} summarizes pooled averages; Table~\ref{tab:crossorgan} combines both
regimes with PCC and MSE.
On HEST-1k \textsc{looo}, \morph{} achieves an average PCC of $0.421$, below Gene-DML
($0.436$) but above HyperST ($0.395$), ST-Net ($0.267$), and Hist2ST ($0.360$). Gene-DML leads on colorectal ($0.738$), pancreas ($0.540$), and skin ($0.766$),
while \morph{} leads on breast, kidney, and prostate. In the pooled regime Gene-DML leads at $0.766$, followed by \morph{} ($0.760$) and HyperST ($0.756$).
To probe cross-species generalization, we additionally evaluate on 59 Visium slides spanning 7 mouse
organs (brain, liver, skin, heart, bowel, kidney, muscle) from the MahmoodLab HEST collection~\cite{jaume2024hest}.
Table~\ref{tab:mouse} reports PCC under the same \textsc{looo} and \textsc{pooled} protocols using mouse-specific 50-gene HVG panels;
\morph{} achieves a pooled PCC of $0.887_{0.005}$ on mouse tissue, with \textsc{looo} and baseline comparisons pending completion.

\begin{table*}[t]
\centering
\caption{\textbf{Cross-organ LOOO transfer, per-organ Pearson $\uparrow$ (HEST-1k).}
Leave-one-organ-out: trained on all other organs, tested on the held-out organ. Eight organ groups;
mean$_{\mathrm{std}}$ over three seeds, nested-validation. Best per row in \best{bold}.}
\label{tab:looo}
\footnotesize
\setlength{\tabcolsep}{3.2pt}
\begin{tabular}{l ccc cc c}
\toprule
Organ & ST-Net~\tiny{'20} & HisToGene~\tiny{'21} & Hist2ST~\tiny{'22} & Gene-DML~\tiny{'26} & HyperST~\tiny{'26} & \morph{} (V3) \\
\midrule
Breast      & $0.217_{0.06}$ & $0.510_{0.02}$ & $0.459_{0.02}$ & $0.521_{0.03}$ & $0.501_{0.02}$ & \best{$0.534_{0.02}$} \\
Colorectal  & $0.430_{0.03}$ & $0.735_{0.01}$ & $0.688_{0.04}$ & \best{$0.737_{0.01}$} & $0.669_{0.06}$ & $0.703_{0.01}$ \\
Kidney      & $0.084_{0.03}$ & $0.064_{0.02}$ & $0.084_{0.01}$ & $0.116_{0.02}$ & $0.108_{0.01}$ & \best{$0.124_{0.00}$} \\
Liver       & $0.024_{0.00}$ & \best{$0.049_{0.01}$} & $0.033_{0.00}$ & $0.045_{0.00}$ & $0.028_{0.00}$ & $0.032_{0.01}$ \\
Lung        & $0.504_{0.04}$ & $0.675_{0.00}$ & $0.499_{0.06}$ & \best{$0.676_{0.01}$} & $0.650_{0.01}$ & $0.660_{0.01}$ \\
Pancreas    & $0.404_{0.05}$ & $0.469_{0.01}$ & $0.360_{0.07}$ & \best{$0.526_{0.03}$} & $0.397_{0.03}$ & $0.507_{0.04}$ \\
Prostate    & $0.071_{0.01}$ & $0.078_{0.01}$ & $0.104_{0.02}$ & $0.110_{0.02}$ & $0.092_{0.03}$ & \best{$0.119_{0.02}$} \\
Skin        & $0.402_{0.08}$ & $0.740_{0.03}$ & $0.652_{0.08}$ & \best{$0.752_{0.02}$} & $0.720_{0.02}$ & $0.692_{0.06}$ \\
\textbf{Average} & $0.267$ & $0.415$ & $0.360$ & \best{$0.435$} & $0.395$ & $0.421$ \\

\bottomrule
\end{tabular}
\end{table*}

\begin{table}[t]
\centering
\caption{\textbf{Cross-organ POOLED, average Pearson $\uparrow$ (HEST-1k).} 5-fold pooled
cross-validation; mean$_{\mathrm{std}}$ over three seeds; nested-validation.
Best in \best{bold}.}
\label{tab:pooled}
\small
\setlength{\tabcolsep}{5pt}
\begin{tabular}{l c}
\toprule
Method & HEST-1k \\
\midrule
ST-Net~\cite{he2020stnet} (2020)\\
HisToGene~\cite{pang2021histogene} (2021)\\
Hist2ST~\cite{zeng2022hist2st} (2022)\\
Gene-DML~\cite{song2026genedml} (2026)    & \best{$0.766_{0.00}$} \\
HyperST~\cite{zhang2026hyperst} (2026)\\
\midrule
\morph{} (V3)\\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]
\centering
\caption{\textbf{Cross-organ generalization} (three seeds, mean$_{\text{std}}$).
\textsc{looo}: leave-one-organ-out transfer; \textsc{pooled}: pooled sample-level cross-validation.
PCC$\uparrow$ / MSE$\downarrow$ on HEST-1k.}
\label{tab:crossorgan}
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{l cc cc}
\toprule
& \multicolumn{2}{c}{\textsc{looo}} & \multicolumn{2}{c}{\textsc{pooled}} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
Method & PCC & MSE & PCC & MSE \\
\midrule
ST-Net~\cite{he2020stnet} (2020)         & \running & \running & \running\\
HisToGene~\cite{pang2021histogene} (2021)   & \running & \running & \running\\
Hist2ST~\cite{zeng2022hist2st} (2022)       & \running & \running & \running\\
Gene-DML~\cite{song2026genedml} (2026)      & $0.435_{0.00}$ & $1.022_{0.02}$ & $0.766_{0.00}$\\
HyperST~\cite{zhang2026hyperst} (2026)      & $0.395_{0.01}$ & $1.136_{0.10}$ & $0.756_{0.01}$\\
\midrule
\morph{} (V3) & $0.421_{0.01}$ & $1.233_{0.11}$ & \running\\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]
\centering
\caption{\textbf{Cross-organ generalization on mouse HEST} (three seeds, mean$_{\text{std}}$).
59 Visium slides across 7 mouse organs (brain, liver, skin, heart, bowel, kidney, muscle).
\textsc{looo}: leave-one-organ-out transfer; \textsc{pooled}: 5-fold pooled cross-validation.
PCC$\uparrow$ on 50 mouse HVGs. Baselines use the same UNI features and protocol as Table~\ref{tab:crossorgan}.}
\label{tab:mouse}
\small
\setlength{\tabcolsep}{5pt}
\begin{tabular}{l c c}
\toprule
Method & \textsc{looo} PCC & \textsc{pooled} PCC \\
\midrule
ST-Net~\cite{he2020stnet} (2020)          & \running & \running \\
HisToGene~\cite{pang2021histogene} (2021) & \running & \running \\
Hist2ST~\cite{zeng2022hist2st} (2022)     & \running & \running \\
TRIPLEX~\cite{chung2024triplex} (2024)    & \running & \running \\
BLEEP~\cite{xie2023bleep} (2023)          & \running & \running \\
\midrule
\morph{} (V3) & \running & $\mathbf{0.887_{0.005}}$ \\
\bottomrule
\end{tabular}
\end{table}

\FloatBarrier
\subsection{RQ3: Which spatial context components drive performance?}
\label{sec:rq3}
\noindent\textbf{Answer: Global and slide context dominate; local attention is additive for intra-cohort but marginal for cross-organ transfer.}
Table~\ref{tab:ablation} reports a factorial study that isolates local attention (L), global attention
(G), and the slide representation (S) rather than adding them in a fixed sequence, evaluated in the
cross-organ \textsc{looo} regime. Global and slide context contribute most: the strongest configuration
is global$+$slide (C011, $0.433$), and every configuration containing G or S improves over the
image-only baseline (C000, $0.417$). Local attention is marginal for cross-organ transfer---adding it
to the full model (C111, $0.421$) does not improve over C011---although it is beneficial in the
intra-cohort regime where the complete model is used. Table~\ref{tab:lossdata} separates the
correlation-aware objective from the architecture: the
correlation term raises both \morph{} and the strongest baseline by a comparable margin ($+0.007$ and
$+0.010$), so it does not account for the architecture gap, and \morph{} transfers to the second
benchmark (pooled PCC $0.652$).

\begin{table}[t]
\centering
\caption{\textbf{Factorial context ablation} (\textsc{looo}, Pearson mean$_{\text{std}}$ over three
seeds). L, G, and S denote local attention, global attention, and slide representation.}
\label{tab:ablation}
\small
\begin{tabular}{l ccc c}
\toprule
Variant & L & G & S & Pearson \\
\midrule
C000 & & & & $0.417_{0.01}$ \\
C001 & & & $\bullet$ & $0.423_{0.01}$ \\
C010 & & $\bullet$ & & $0.420_{0.01}$ \\
C011 & & $\bullet$ & $\bullet$ & \best{$0.433_{0.00}$} \\
C100 & $\bullet$ & & & $0.396_{0.01}$ \\
C101 & $\bullet$ & & $\bullet$ & $0.427_{0.00}$ \\
C110 & $\bullet$ & $\bullet$ & & $0.416_{0.02}$ \\
C111 & $\bullet$ & $\bullet$ & $\bullet$ & $0.421_{0.01}$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]
\centering
\caption{\textbf{Objective ablation (HEST-1k).} Effect of adding the Pearson correlation term to the
MSE objective, averaged over eight organ groups; three seeds.}
\label{tab:lossdata}
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{l cc}
\toprule
Experiment & Setting & Pearson \\
\midrule
HEST loss & \morph, MSE only & $0.382$ \\
          & \morph, MSE$+$PCC & $0.389$ \\
\bottomrule
\end{tabular}
\end{table}

\FloatBarrier
\subsection{RQ4: Do predictions recover biological spatial structure beyond per-gene correlation?}
\label{sec:rq4}
\noindent\textbf{Answer: Yes---predicted expression maps recover tissue spatial domains at parity with the strongest baseline.}
Scalar correlation measures per-gene accuracy but not whether predictions are spatially coherent.
We probe this in two ways.
First, Figure~\ref{fig:umap} visualizes \morph{} slide representations colored by organ,
checking whether the coordinate-invariant pathway yields organ-transferable features without supervision.
Second, we cluster spots by predicted and by measured expression and compare the resulting spatial
domains using ARI and NMI; high agreement means predictions recover tissue structure, not just
per-gene trends (Table~\ref{tab:downstream}).
All methods recover substantial structure (ARI well above chance), and \morph{} matches the
strongest baseline, confirming its accuracy advantage does not come at the expense of spatial coherence.
Figure~\ref{fig:qual} gives a qualitative view: predicted expression maps for representative
marker genes on held-out slides reproduce spatial gradients and tissue-structured patterns
rather than a spatially uniform mean.

\begin{figure}[t]
\centering
\figph{3.4cm}{[UMAP of learned slide representations colored by organ --- generated from the
completed corrected checkpoints.]}
\caption{\textbf{Learned representation.} UMAP of \morph{} slide representations, colored by organ.
Generated from the corrected checkpoints once training completes.}
\label{fig:umap}
\end{figure}

\begin{table}[t]
\centering
\caption{\textbf{Downstream spatial domain recovery.} Agreement between spatial domains clustered from
predicted vs.\ measured expression on held-out slides (higher is better). ARI/NMI over held-out
slides; three seeds.}
\label{tab:downstream}
\small
\setlength{\tabcolsep}{6pt}
\begin{tabular}{l cc}
\toprule
Method & ARI $\uparrow$ & NMI $\uparrow$ \\
\midrule
ST-Net~\cite{he2020stnet} (2020)         & $0.148_{0.13}$\\
HisToGene~\cite{pang2021histogene} (2021)   & \running\\
Hist2ST~\cite{zeng2022hist2st} (2022)       & \running\\
Gene-DML~\cite{song2026genedml} (2026)      & \running\\
HyperST~\cite{zhang2026hyperst} (2026)      & \running\\
\midrule
\morph{} (V3) & $0.170_{0.15}$\\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\figph{3.8cm}{[Predicted vs.\ measured spatial expression maps for marker genes on held-out slides
--- generated from the saved test predictions.]}
\caption{\textbf{Predicted expression maps.} Measured (top) and \morph{}-predicted (bottom) spatial
expression of marker genes on held-out slides. Generated from the saved corrected test predictions.}
\label{fig:qual}
\end{figure}

\FloatBarrier
\subsection{RQ5: How efficient and scalable is the model?}
\label{sec:rq5}
\noindent\textbf{Answer: \morph{} uses $4.8$M parameters and one forward pass, scaling to 10{,}000 spots in 65\,ms on an A100.}
Table~\ref{tab:complexity} compares parameter counts and inference procedures. \morph{} contains
$4.75$M parameters, compared with $0.05$M for ST-Net and $149$M for HisToGene. It produces a
slide-level prediction in one forward pass; its primary efficiency advantage is its simple inference
procedure rather than the smallest parameter count. Table~\ref{tab:scaling} reports measured
model-only inference scaling.

\begin{table}[t]
\centering
\caption{\textbf{Model complexity.} Parameter counts and inference mechanisms. \morph{} predicts in
a single forward pass. Maximum parameter count in \best{bold}.}
\label{tab:complexity}
\small
\setlength{\tabcolsep}{5pt}
\begin{tabular}{l r l}
\toprule
Model & \#Params (M) & Inference \\
\midrule
ST-Net~\cite{he2020stnet} (2020)      & 0.05  & single-pass \\
HisToGene~\cite{pang2021histogene} (2021)   & \best{149.0} & single-pass \\
\midrule
\morph{} (V3) & 4.75 & single-pass \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]
\centering
\caption{\textbf{MorphoST inference scaling on an NVIDIA A100 80GB.} Median model-only latency and
peak allocated GPU memory over synthetic slides; feature extraction and disk I/O are excluded.}
\label{tab:scaling}
\small
\begin{tabular}{r rr}
\toprule
Spots & Latency (ms) & Memory (MiB) \\
\midrule
500    & 3.48  & 43 \\
1,000  & 3.63  & 67 \\
3,000  & 9.36  & 335 \\
5,000  & 21.31 & 842 \\
10,000 & \best{64.74} & \best{3,183} \\
\bottomrule
\end{tabular}
\end{table}

\FloatBarrier
\section{Discussion}
\label{sec:discussion}
The experimental design tests two hypotheses: whether direct regression can attain strong accuracy
under matched pathology features and splits, both within a cohort and under organ shift, and which
combination of local, global, and slide-level context is most effective.
Final conclusions will be drawn from the completed nested-validation runs. Independently of predictive
accuracy, the architecture retains single-pass inference and coordinate-system invariance, and the
representation and spatial domain analyses probe whether its predictions are structured rather than
merely well correlated on average.

\textbf{Limitations.} First, \morph{} uses a correlation-aware objective, whereas the regression
baselines retain MSE; Table~\ref{tab:lossdata} isolates this factor, but a fully controlled comparison
should evaluate all architectures under both losses. Second, all methods use frozen UNI embeddings and
cannot recover information omitted by the feature encoder. Third, the reported experiments use a
50-gene prediction task; larger panels and additional platforms, staining variation, and institutions
are needed to establish broader generalization. The coordinate-invariance result does not imply
rotation invariance of the frozen histology encoder. Finally, the pooled cross-organ split is
sample-disjoint but cannot be verified as patient-disjoint for cohorts without patient metadata.

\section{Conclusion}
We introduced \morph, a direct-regression transformer for spatial gene expression prediction from
histology. The model combines distance-biased local attention, global self-attention, and a
slide-level representation; its coordinate pathway is invariant to rotations, reflections, and translations. Under a
unified feature-matched protocol we evaluate \morph{} in two regimes---intra-cohort and cross-organ
transfer---on HEST-1k against direct, retrieval-based, and attention-based baselines,
with three metrics, a factorial ablation, representation and spatial domain analyses, and measured
scaling. Together these establish when direct spatial regression is a simple, reproducible, and
generalizable alternative to generative prediction.

\appendix

\section{Numerical invariance verification}
\label{app:invariance}
We verify the spatial coordinate invariance independently of predictive accuracy. For a randomly
initialized evaluation mode model and a synthetic 257-spot slide, we transform only the coordinates
while holding the UNI features fixed. The maximum absolute output deviations are
$5.97\!\times\!10^{-5}$ for translation, $1.97\!\times\!10^{-5}$ for rotation, exactly zero for
reflection, and $5.96\!\times\!10^{-7}$ after permuting and restoring the spot order. These small
nonzero deviations arise from floating point distance computation. This test verifies the coordinate
pathway; it does not test rotation invariance of the frozen histology encoder.

\begin{table}[h]
\centering
\caption{\textbf{Numerical coordinate-invariance check.} Maximum absolute prediction difference.}
\label{tab:invariance}
\small
\begin{tabular}{l r}
\toprule
Transformation & Max. absolute difference \\
\midrule
Translation & $5.97\times10^{-5}$ \\
Rotation & $1.97\times10^{-5}$ \\
Reflection & $0$ \\
Spot permutation & $5.96\times10^{-7}$ \\
\bottomrule
\end{tabular}
\end{table}

\section{Full per-cohort results}
Table~\ref{tab:percohort} reserves the full ten-cohort corrected comparison before the organ-level
aggregation used in Table~\ref{tab:main}.

\begin{table*}[t]
\centering
\caption{\textbf{Full per-cohort results on HEST-1k} (Pearson mean$_{\text{std}}$ over three seeds;
nested-validation), before the organ-level aggregation of Table~\ref{tab:main}. Best per row in
\best{bold}. \morph{} leads on all ten cohorts. Published per-cohort results are summarized in
Appendix~\ref{app:published}.}
\label{tab:percohort}
\footnotesize
\setlength{\tabcolsep}{3.2pt}
\begin{tabular}{l ccc cc c}
\toprule
Cohort & ST-Net~\tiny{'20} & HisToGene~\tiny{'21} & Hist2ST~\tiny{'22} & Gene-DML~\tiny{'26} & HyperST~\tiny{'26} & \morph{} (V3) \\
\midrule
IDC       & $0.538_{0.01}$ & $0.447_{0.08}$ & $0.250_{0.07}$ & $0.557_{0.01}$ & $0.558_{0.01}$ & \best{$0.567_{0.00}$} \\
PRAD      & $0.251_{0.01}$ & $0.332_{0.00}$ & $0.317_{0.02}$ & $0.363_{0.01}$ & $0.369_{0.00}$ & \best{$0.390_{0.02}$} \\
PAAD      & $0.353_{0.02}$ & $0.307_{0.03}$ & $0.243_{0.06}$ & $0.362_{0.04}$ & $0.367_{0.04}$ & \best{$0.392_{0.04}$} \\
SKCM      & $0.632_{0.01}$ & $0.554_{0.01}$ & $0.509_{0.02}$ & $0.674_{0.00}$ & $0.641_{0.00}$ & \best{$0.696_{0.00}$} \\
COAD      & $0.209_{0.02}$ & $0.252_{0.01}$ & $0.293_{0.01}$ & $0.276_{0.01}$ & $0.279_{0.01}$ & \best{$0.299_{0.00}$} \\
READ      & $0.154_{0.00}$ & $0.128_{0.01}$ & $0.097_{0.01}$ & $0.168_{0.01}$ & $0.168_{0.01}$ & \best{$0.187_{0.02}$} \\
CCRCC     & $0.172_{0.01}$ & $0.210_{0.01}$ & $0.217_{0.01}$ & $0.224_{0.03}$ & $0.256_{0.00}$ & \best{$0.265_{0.02}$} \\
HCC       & $0.056_{0.01}$ & $0.029_{0.00}$ & $0.031_{0.01}$ & $0.077_{0.01}$ & $0.087_{0.01}$ & \best{$0.100_{0.00}$} \\
LUNG      & $0.482_{0.01}$ & $0.490_{0.00}$ & $0.378_{0.10}$ & $0.564_{0.00}$ & $0.581_{0.01}$ & \best{$0.589_{0.01}$} \\
LYMPH\_IDC & $0.191_{0.02}$ & $0.279_{0.01}$ & $0.304_{0.00}$ & $0.234_{0.00}$ & $0.238_{0.01}$ & \best{$0.312_{0.01}$} \\
\bottomrule
\end{tabular}
\end{table*}

\section{Published Per-Cohort Comparison}
\label{app:published}

\begin{table}[h]
\centering
\caption{\textbf{Published per-cohort accuracy on the standard HEST-1k splits}, mean PCC$\uparrow$
as reported in respective papers (ten cohorts, no organ grouping). Shown for context
only---\emph{not comparable} to Table~\ref{tab:main}, which uses organ grouping and
nested-validation. $\dagger$\morph{} uses our organ-grouped splits, not the standard per-cohort split.}
\label{tab:published}
\small
\begin{tabular}{lc}
\toprule
Method & HEST-1k (10 cohorts) \\
\midrule
ST-Net~\cite{he2020stnet} (2020)            & $0.286$ \\
HisToGene~\cite{pang2021histogene} (2021)   & $0.237$ \\
Hist2ST~\cite{zeng2022hist2st} (2022)       & \running \\
Gene-DML~\cite{song2026genedml} (2026)      & \running \\
HyperST~\cite{zhang2026hyperst} (2026)      & \running \\
MCToGene~\cite{sun2026mctogene} (2026)      & \best{$0.435$} \\
\midrule
\morph{}$^\dagger$ (ours)                   & $0.440$ \\
\bottomrule
\end{tabular}
\end{table}

{\small
\bibliographystyle{ieeenat_fullname}
\bibliography{references}}

\end{document}
